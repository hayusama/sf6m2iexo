<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid px-4">
      <a class="navbar-brand" href="{{path('homepage')}}">Blog programmation</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse flex-row-reverse" id="navbarNav">
        <ul class="navbar-nav ">
          {% for menu in listMenu %}
            {% if app.user and (menu.user is not defined or menu.user is same as (true)) %}
              <li class="nav-item  ">
                  <a class="nav-link {% if loop.index == 1 %} active {% endif %}" href="{{menu.url}}" title="{{menu.title}}">{{menu.text}}</a>
              </li>
            {% elseif app.user is null and (menu.user is not defined or menu.user is same as (false))%}
              <li class="nav-item  ">
                <a class="nav-link {% if loop.index == 1 %} active {% endif %}" href="{{menu.url}}" title="{{menu.title}}">{{menu.text}}</a>
              </li>
            {% endif %}
          {% endfor %}
          {% if is_granted('ROLE_ADMIN') %}
              <li><a href="{{path('app_admin')}}" class="nav-link badge bg-danger">Administation</a></li>
          {% endif %}
        </ul>
      </div>
    </div>
  </nav>